import { SetHeaderPipe } from './set-header.pipe';

describe('SetHeaderPipe', () => {
  it('create an instance', () => {
    const pipe = new SetHeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
